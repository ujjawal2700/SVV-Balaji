name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: svv_admin
          POSTGRES_PASSWORD: changeme
          POSTGRES_DB: svv_balaji_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U svv_admin"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 5

    env:
      DATABASE_URL: postgresql://svv_admin:changeme@localhost:5432/svv_balaji_test?schema=public
      JWT_ACCESS_SECRET: ci_test_secret
      JWT_REFRESH_SECRET: ci_test_refresh_secret

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "npm"

      - run: npm install

      - run: npx prisma generate

      - run: npx prisma migrate deploy

      - run: npm run lint

      - run: npm run build

      - run: npm run test
